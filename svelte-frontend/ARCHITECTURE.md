# 🏗️ АРХИТЕКТУРА ПРОЕКТА

## 📐 СХЕМА ВЗАИМОДЕЙСТВИЯ

```
┌─────────────────────────────────────────────────────┐
│                    BROWSER                           │
│                http://localhost:5173                 │
└──────────────────────┬──────────────────────────────┘
                       │
                       │ HTTP Requests
                       │ (fetch API)
                       ↓
┌─────────────────────────────────────────────────────┐
│           SvelteKit Frontend (Port 5173)             │
│                                                       │
│  ┌─────────────────────────────────────────────┐   │
│  │           UI Components (Svelte 5)          │   │
│  │  - Navbar (Lucide icons, i18n, mobile)      │   │
│  │  - Login page                                │   │
│  │  - Register page                             │   │
│  │  - Webhooks viewer                           │   │
│  └─────────────────────────────────────────────┘   │
│                       │                              │
│  ┌─────────────────────────────────────────────┐   │
│  │           Stores (Runes)                     │   │
│  │  - authState ($state, $derived)             │   │
│  │  - webhooksState                             │   │
│  │  - i18n (RU/EN)                              │   │
│  └─────────────────────────────────────────────┘   │
│                       │                              │
│  ┌─────────────────────────────────────────────┐   │
│  │           API Client                         │   │
│  │  src/lib/utils/api.ts                        │   │
│  │  - api.login()                               │   │
│  │  - api.getWebhooks()                         │   │
│  │  - api.deleteWebhook()                       │   │
│  │  - api.approveUser()                         │   │
│  └─────────────────────────────────────────────┘   │
│                       │                              │
└───────────────────────┼──────────────────────────────┘
                        │
                        │ fetch('http://localhost:3000/api/...')
                        │ Headers: { Authorization: 'Bearer token' }
                        ↓
┌─────────────────────────────────────────────────────┐
│           Fastify Backend (Port 3000)                │
│                                                       │
│  ┌─────────────────────────────────────────────┐   │
│  │           API Routes                         │   │
│  │  /api/auth/login                             │   │
│  │  /api/auth/register                          │   │
│  │  /api/auth/me                                │   │
│  │  /api/webhook/:id                            │   │
│  │  /api/room/:id                               │   │
│  │  /api/admin/*                                │   │
│  │  /hook/:id (webhook receiver)                │   │
│  └─────────────────────────────────────────────┘   │
│                       │                              │
│  ┌─────────────────────────────────────────────┐   │
│  │           Services                           │   │
│  │  - AuthService (JWT, bcrypt)                │   │
│  │  - RoomRepository                            │   │
│  │  - WebhookRepository                         │   │
│  └─────────────────────────────────────────────┘   │
│                       │                              │
│  ┌─────────────────────────────────────────────┐   │
│  │           MongoDB                            │   │
│  │  - users collection                          │   │
│  │  - rooms collection                          │   │
│  │  - webhooks collection                       │   │
│  └─────────────────────────────────────────────┘   │
│                                                       │
└─────────────────────────────────────────────────────┘
                        │
                        │ TCP Connection
                        ↓
┌─────────────────────────────────────────────────────┐
│              MongoDB (Port 27017)                    │
│                                                       │
│  Database: webhook_viewer                            │
│  Collections:                                         │
│    - users (username, email, password_hash, role)    │
│    - rooms (roomId, userId, webhookTTL)              │
│    - webhooks (roomId, body, timestamp)              │
│                                                       │
└─────────────────────────────────────────────────────┘
```

---

## 🔄 FLOW: Как работает логин

```
1. User вводит логин/пароль в SvelteKit
   ↓
2. SvelteKit вызывает api.login(username, password)
   ↓
3. api.ts делает fetch('http://localhost:3000/api/auth/login')
   ↓
4. Fastify получает запрос
   ↓
5. authService.login() проверяет пароль (bcrypt)
   ↓
6. Fastify генерирует JWT токен
   ↓
7. Fastify возвращает { token, user }
   ↓
8. api.ts получает ответ
   ↓
9. authState.login() сохраняет token в localStorage
   ↓
10. User перенаправляется на главную страницу
```

---

## 🔄 FLOW: Как работает загрузка webhooks

```
1. User вводит roomId и нажимает "Загрузить"
   ↓
2. webhooksState.fetchWebhooks(roomId)
   ↓
3. api.getWebhooks(roomId) → fetch('http://localhost:3000/api/webhook/room123')
   ↓
4. Fastify получает запрос
   ↓
5. webhookRepo.getWebhooks(roomId) → MongoDB query
   ↓
6. MongoDB возвращает массив webhooks
   ↓
7. Fastify возвращает JSON
   ↓
8. api.ts парсит ответ
   ↓
9. webhooksState.items = webhooks
   ↓
10. Svelte re-renders UI
```

---

## 📂 ФАЙЛОВАЯ СТРУКТУРА

```
PROJECT/
│
├── sveltekit-frontend/              # Этот проект
│   ├── src/
│   │   ├── lib/
│   │   │   ├── components/
│   │   │   │   └── Navbar.svelte    ← UI компонент
│   │   │   ├── stores/
│   │   │   │   ├── auth.svelte.ts   ← State management
│   │   │   │   ├── webhooks.svelte.ts
│   │   │   │   └── i18n.svelte.ts
│   │   │   ├── utils/
│   │   │   │   └── api.ts           ← HTTP client ⭐
│   │   │   └── types/
│   │   │       └── index.ts
│   │   └── routes/
│   │       ├── +layout.svelte
│   │       ├── +page.svelte         ← Главная
│   │       ├── login/+page.svelte
│   │       └── register/+page.svelte
│   ├── .env                          ← VITE_API_URL
│   └── package.json                  ← lucide-svelte
│
└── fastify-backend/                 # Ваш существующий backend
    ├── src/
    │   ├── routes/
    │   │   ├── authRoutes.ts        ← /api/auth/*
    │   │   ├── roomRoutes.ts        ← /api/room/*
    │   │   ├── webhookRoutes.ts     ← /api/webhook/*
    │   │   └── adminRoutes.ts       ← /api/admin/*
    │   ├── auth/
    │   │   ├── middleware.ts        ← JWT verification
    │   │   └── service.ts
    │   ├── repositories/
    │   │   ├── RoomRepository.ts
    │   │   └── WebhookRepository.ts
    │   └── index.ts                 ← Fastify app
    ├── .env                          ← MONGO_URI, JWT_SECRET
    └── package.json                  ← fastify, mongodb, jwt
```

---

## 🔐 AUTHENTICATION FLOW

```
┌──────────────┐
│   Browser    │
└──────┬───────┘
       │ 1. POST /api/auth/login
       │    { username, password }
       ↓
┌──────────────────┐
│   api.ts         │
│   (Frontend)     │
└──────┬───────────┘
       │ 2. fetch with JSON body
       ↓
┌────────────────────────────┐
│   Fastify                  │
│   authRoutes.ts            │
│   1. Validate input        │
│   2. Find user in MongoDB  │
│   3. Compare password      │
│   4. Generate JWT          │
└──────┬─────────────────────┘
       │ 3. { token, user }
       ↓
┌──────────────────┐
│   authState      │
│   (Store)        │
│   1. Save token  │
│   2. Save user   │
│   3. Update UI   │
└──────────────────┘
```

### Subsequent requests:

```
Browser → api.ts → Fastify
          ↑
          │ Headers: {
          │   Authorization: 'Bearer <token>'
          │ }
          ↓
     Fastify middleware verifies JWT
     → Attaches user to request
     → Processes request
```

---

## 🌐 i18n FLOW

```
┌─────────────┐
│   User      │
│   clicks 🌐 │
└──────┬──────┘
       │
       ↓
┌──────────────────┐
│   i18n.svelte.ts │
│   toggleLocale() │
│   RU ⇄ EN        │
└──────┬───────────┘
       │
       ↓ Reactive
┌──────────────────┐
│   All components │
│   re-render with │
│   new translations│
└──────────────────┘
```

**Translations stored in:**
- `src/lib/stores/i18n.svelte.ts`
- Saved to `localStorage`

---

## 📱 RESPONSIVE DESIGN

```
Mobile (< 640px)
├── Burger menu ☰
├── Small logo
├── Vertical layout
└── Touch-friendly buttons

Tablet (640px - 1024px)
├── Partial navbar
├── Adaptive grid
└── Medium buttons

Desktop (> 1024px)
├── Full navbar
├── User dropdown
├── Hover effects
└── Desktop grid
```

---

## 🔄 DATA FLOW DIAGRAM

```
┌────────────┐
│  UI Event  │  (button click)
└─────┬──────┘
      │
      ↓
┌─────────────┐
│  Store      │  (webhooksState.fetchWebhooks)
└─────┬───────┘
      │
      ↓
┌─────────────┐
│  API Client │  (api.getWebhooks)
└─────┬───────┘
      │
      ↓
┌─────────────┐
│  HTTP       │  (fetch)
└─────┬───────┘
      │
      ↓
┌─────────────┐
│  Fastify    │  (route handler)
└─────┬───────┘
      │
      ↓
┌─────────────┐
│  Repository │  (webhookRepo.getWebhooks)
└─────┬───────┘
      │
      ↓
┌─────────────┐
│  MongoDB    │  (db.collection.find)
└─────┬───────┘
      │
      ↓
┌─────────────┐
│  Response   │  (JSON)
└─────┬───────┘
      │
      ↓
┌─────────────┐
│  Store      │  (update state)
└─────┬───────┘
      │
      ↓
┌─────────────┐
│  UI Update  │  (Svelte reactivity)
└─────────────┘
```

---

## 🚀 DEPLOYMENT

### Development:

```bash
# Terminal 1: Backend
cd fastify-backend
npm run dev         # :3000

# Terminal 2: Frontend
cd sveltekit-frontend
npm run dev         # :5173
```

### Production:

```bash
# Backend
cd fastify-backend
npm run build
npm start           # :3000

# Frontend
cd sveltekit-frontend
npm run build
npm run preview     # :4173

# Or use adapter-node for SSR
```

---

## 🔒 SECURITY

```
┌─────────────────────────────────┐
│  JWT Token Flow                  │
│                                  │
│  1. Login → Fastify generates   │
│     JWT with secret              │
│                                  │
│  2. Frontend saves in            │
│     localStorage                 │
│                                  │
│  3. Every request includes:      │
│     Authorization: Bearer token  │
│                                  │
│  4. Fastify middleware verifies  │
│     with JWT secret              │
│                                  │
│  5. If invalid → 401             │
│     Frontend clears token        │
└─────────────────────────────────┘
```

---

## 📊 TECH STACK

```
Frontend (SvelteKit):
├── Svelte 5.0          (Runes: $state, $derived)
├── SvelteKit 2.5       (SSR, routing)
├── TypeScript 5.0      (Type safety)
├── Lucide-svelte       (Icons)
├── TailwindCSS         (Styling)
└── Vite 5.0            (Build tool)

Backend (Fastify):
├── Fastify             (HTTP server)
├── MongoDB             (Database)
├── JWT                 (Auth tokens)
├── bcryptjs            (Password hashing)
└── TypeScript          (Type safety)
```

---

**Готово! Полная архитектура проекта! 🏗️**
